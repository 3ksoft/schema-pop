{
	"name": "typescript",
	"type": "module",
	"scripts": {
		"build": "bun build --compile src/run-abi-test.ts --outfile harness",
		"test": "bun run src/run-abi-test.ts"
	},
	"dependencies": {
		"schema-pop": "latest",
		"@schema-pop/core-exporters": "latest"
	},
	"devDependencies": {
		"@types/bun": "^1.3.12",
		"@types/node": "^25.6.0"
	}
}
