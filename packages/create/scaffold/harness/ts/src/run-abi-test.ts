// Placeholder ABI consistency test runner. Real implementation lives in
// `bridge-gen.ts` / per-language harnesses. The scaffold ships a stub so
// `bun run test` doesn't error on a fresh project.
console.log("schema-pop ts harness — no ABI test wired yet");
