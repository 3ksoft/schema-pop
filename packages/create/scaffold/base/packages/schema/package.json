{
	"name": "schema-pop-test",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"generate": "bunx schema-pop pop.config.ts",
		"build": "bun run generate && tsc"
	},
	"devDependencies": {
		"schema-pop": "latest",
		"@schema-pop/schema": "latest",
		"@schema-pop/exporter": "latest",
		"@types/node": "^25.6.0"
	}
}
