{
	"name": "schema-pop-import",
	"version": "0.1.32",
	"description": "Top-level alias so `bunx schema-pop-import <file>` resolves without the `@schema-pop/importer` scope. Thin shim — all logic lives in @schema-pop/importer.",
	"type": "module",
	"bin": {
		"schema-pop-import": "bin.ts"
	},
	"files": [
		"bin.ts",
		"README.md"
	],
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@schema-pop/importer": "0.1.32"
	}
}
