{
	"name": "schema-pop",
	"version": "0.2.0",
	"type": "module",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/3ksoft/schema-pop.git"
	},
	"bugs": {
		"url": "https://github.com/3ksoft/schema-pop/issues"
	},
	"homepage": "https://github.com/3ksoft/schema-pop#readme",
	"keywords": [
		"schema",
		"serialization",
		"binary",
		"layout",
		"compiler",
		"codegen",
		"cli"
	],
	"bin": {
		"schema-pop": "./dist/cli.js"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"build:cli": "bun build ./src/cli.ts --outdir ./dist --target node --packages external",
		"build:lib": "bun build ./src/index.ts --outdir ./dist --target node --packages external && tsgo --declaration --emitDeclarationOnly --outDir dist --noEmit false",
		"build": "rm -rf dist && bun run build:cli && bun run build:lib",
		"typecheck": "tsgo --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@ark/schema": "0.56.0",
		"@schema-pop/schema": "workspace:*",
		"@schema-pop/core": "workspace:*",
		"@schema-pop/exporter": "workspace:*",
		"cac": "^7.0.0"
	},
	"devDependencies": {
		"@types/node": "^25.6.2",
		"typescript": "7.0.2"
	},
	"files": [
		"dist",
		"src"
	]
}