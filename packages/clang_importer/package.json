{
	"name": "@schema-pop/clang-importer",
	"version": "0.1.25",
	"description": "Import C / C++ headers into schema-pop via the system clang's JSON AST dump (experimental).",
	"type": "module",
	"main": "./dist/index.js",
	"module": "./dist/index.js",
	"bun:main": "./src/index.ts",
	"types": "./dist/index.d.ts",
	"bin": {
		"schema-pop-clang-import": "./src/cli.ts"
	},
	"files": [
		"dist",
		"src"
	],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "rm -rf dist && bun build ./src/index.ts --outdir ./dist --target node --packages external && tsc --declaration --emitDeclarationOnly --outDir dist --noEmit false",
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@schema-pop/treesitter-importer": "0.1.25",
		"schema-pop": "0.1.25"
	}
}
