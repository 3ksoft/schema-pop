{
	"name": "schema-pop-monorepo",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build:schema": "cd packages/schema && bun run build && bun link && cd ../..",
		"build:exporter": "cd packages/exporter && bun run build && bun link && cd ../..",
		"build:core": "cd packages/core && bun run build && bun link && cd ../..",
		"build": "bun build:schema && bun run build:core && bun build:exporter",
		"typecheck": "cd packages/schema && bun run typecheck && cd ../core && bun run typecheck && cd ../exporter && bun run typecheck && cd ../tests && bun run typecheck",
		"test": "cd packages/core && bun test",
		"format": "biome format --write",
		"format:check": "biome format",
		"release": "scripts/publish.sh",
		"deploy:pages": "scripts/deploy-gh-pages.sh",
		"node:cleanup": "rm -rf $(fd --no-ignore tsconfig.tsbuildinfo) && rm -rf $(fd --no-ignore bun.lock) && rm -rf $(fd --no-ignore node_modules) && bun pm cache rm && bun i"
	},
	"devDependencies": {
		"@types/node": "^26.1.2"
	}
}